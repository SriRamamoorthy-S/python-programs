import textwrap

def wrap(string, max_width):
    # textwrap.fill wraps the text and joins the lines with \n automatically
    return textwrap.fill(string, max_width)

if __name__ == '__main__':
    string = input()
    max_width = int(input())
    result = wrap(string, max_width)
    print(result)
